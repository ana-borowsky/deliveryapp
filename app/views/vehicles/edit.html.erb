<section class='page-layout'>
<% if(@vehicle.errors.any?) %>
  <p>Verifique os erros abaixo:</p>
  <ul>
    <% @vehicle.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
  </ul>
<% end %>
<h2>Editar veículo</h2>
<div class='page-title'>
  <%= link_to 'Voltar', vehicle_path, class: 'button' %>
</div>
<%= render 'form' %>

<section class='page-layout'>