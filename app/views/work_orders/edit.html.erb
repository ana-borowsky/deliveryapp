<section class='page-layout'>
<% if(@work_order.errors.any?) %>
  <p>Verifique os erros abaixo:</p>
  <ul>
    <% @work_order.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
  </ul>
<% end %>
<h2>Editar ordem de serviço <%= @work_order.code %> </h2>
<div class='page-title-two-buttons'>
  <%= link_to 'Voltar', work_order_path, class: 'button', class: 'button' %>
</div>
<%= render 'form' %>
</section>