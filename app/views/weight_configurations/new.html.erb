<% if(@weight_configuration.errors.any?) %>
  <p>Verifique os erros abaixo:</p>
  <ul>
    <% @weight_configuration.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
  </ul>
<% end %>
<h2>Nova configuração de preço por peso</h2>
<%= render 'form' %><br>
<%= link_to 'Voltar', weight_configurations_path %>